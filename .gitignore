# Secrets — never commit credentials
.env
.env.*
!.env.example
*.local

# Reference books — copyrighted, and large. Keep them locally, not in git.
books/

# IcePanel / tooling
node_modules/

# OS
.DS_Store
